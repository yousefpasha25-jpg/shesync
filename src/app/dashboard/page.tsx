import Dashboard from "@/features/dashboard/Dashboard";

export const metadata = {
  title: "Dashboard | SheSync",
  description: "Your personal AI fitness dashboard. Track workouts, nutrition, and wellness — all synced to your cycle.",
};

export default function DashboardPage() {
  return <Dashboard />;
}
