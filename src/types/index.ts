// Shared TypeScript types
export interface User {
  id: string;
  email: string;
  fullName: string;
  avatarUrl?: string;
  isOnboardingComplete: boolean;
  createdAt: string;
  updatedAt: string;
}
